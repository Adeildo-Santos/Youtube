services:
  - type: web
    name: youtube-downloader
    env: node
    plan: free
    buildCommand: npm install && apt-get update && apt-get install -y yt-dlp ffmpeg
    startCommand: npm start
    healthCheckPath: /api/health
