{
  "name": "youtube-downloader",
  "version": "1.0.0",
  "description": "YouTube Downloader com Express e yt-dlp",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "postinstall": "pip install yt-dlp --break-system-packages 2>/dev/null || apt-get update && apt-get install -y yt-dlp ffmpeg 2>/dev/null || true"
  },
  "keywords": [
    "youtube",
    "downloader",
    "yt-dlp"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
